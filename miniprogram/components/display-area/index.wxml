<view class="video-container">
  <video 
    id="myVideo"
    class="video" 
    src="{{videoPath}}"
    autoplay="{{true}}"
    loop="{{true}}"
    controls="{{true}}"
    show-center-play-btn="{{true}}"
    show-play-btn="{{true}}"
    object-fit="contain"
    binderror="handleVideoError"
  ></video>
  <view class="description">
    <text class="{{isWelcomeText ? 'welcome-text' : 'normal-text'}}">{{description}}</text>
  </view>
</view> 