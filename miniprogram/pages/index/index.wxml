<view class="container">
  <display-area 
    currentImage="{{currentImage}}"
    description="{{description}}"
  />
  <voice-button 
    text="{{buttonText}}"
    bindrecordingStateChange="handleRecordingStateChange"
    bindrecordingError="handleRecordingError"
  />
</view>