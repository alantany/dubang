<view class="container">
  <display-area 
    currentImage="{{currentImage}}"
    description="{{description}}"
  />
  <button 
    class="test-button"
    bindtap="handleTap"
  >{{buttonText}}</button>
</view>